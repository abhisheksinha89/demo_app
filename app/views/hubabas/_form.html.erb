<%= form_for(@hubaba) do |f| %>
  <% if @hubaba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hubaba.errors.count, "error") %> prohibited this hubaba from being saved:</h2>

      <ul>
      <% @hubaba.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :man %><br>
    <%= f.text_field :man %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
